
import Foundation

final class League {
    let minimum: Int
    let title: String
    let id: Int
    
    init(minimum: Int, title: String, id: Int) {
        self.minimum = minimum
        self.title = title
        self.id = id
    }
}
